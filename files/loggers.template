[loggers]
keys = root

[handlers]
keys = file

[formatters]
keys = full

[logger_root]
level = INFO
handlers = file

[handler_file]
class = FileHandler
args = ('%(LOGFILE)',)		# The output file is speficied HERE
formatter = full

[formatter_full]
format = %(asctime)s - %(levelname)s: %(message)s
